includes:
    - vendor/nunomaduro/larastan/extension.neon

parameters:

    paths:
        - src/

    # Level 5 is what you requested
    level: 5

    # ignoreErrors:
    #     - '#Unsafe usage of new static\(\)#'

    excludePaths:
        - ./*/*/FileToBeExcluded.php

    checkMissingIterableValueType: false
